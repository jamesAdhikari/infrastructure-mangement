---
- name: deploy and run artifact in servers
  hosts: dev
  tasks:
    - name: copy arrifact to servers
      copy:
        src: '/tmp/workspace/test-pipeline/spring-boot-rest-example/target/spring-boot-rest-example-0.5.0.war'
        dest: '~/'

    - name: run artifact in server
      shell: cd ~/; nohup java -jar spring-boot-rest-example-0.5.0.war > /dev/null 2>&1 &
